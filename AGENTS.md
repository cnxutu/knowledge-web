# AGENTS

## 作用范围

本文件对整个 `knowledge-web` 仓库生效；如果子目录下存在更深层的 `AGENTS.md`，则以更近的文件为准。

## 仓库定位

- `knowledge-web` 是工作区中的前端代码仓，对应工作区级别名中的“项目B”。
- 当前仓库包含两个前端子项目：
  - `kw-admin-vue`：后台管理端前端项目。
  - `kw-frontend-react`：前台展示端前端项目。
- 如果需求主要是页面、交互、路由、状态管理、前端接口联调或前端构建配置，通常应优先落在本仓库。

## 顶层项目地图

### `kw-admin-vue`

- 定位：后台管理端。
- 适合放置：
  - 管理后台页面、菜单、权限相关前端逻辑
  - 表单、表格、详情页、运营配置类界面
  - 管理端接口联调、请求封装与后台工作流交互

### `kw-frontend-react`

- 定位：前台展示端。
- 适合放置：
  - 面向终端用户的展示页面与交互流程
  - 前台路由、状态管理、主题样式与组件编排
  - 面向用户访问场景的接口展示与页面联调

## 典型改动应该落在哪

### 后台运营、管理配置、内部工作台页面

- 优先落在 `kw-admin-vue`。

### 前台门户、内容展示、用户访问页面

- 优先落在 `kw-frontend-react`。

### 同时涉及前后端接口字段或业务规则

- 先确认前端改动落点，再同步检查 `knowledge-hub` 中对应后端模块是否需要一起调整。

## 在本仓库中的协作方式

- 先确认需求属于后台管理端还是前台展示端，不要在错误子项目里临时落实现。
- 优先遵守目标子项目现有的目录组织、组件模式、状态管理方式和请求封装约定。
- 页面逻辑、样式实现与局部交互尽量收敛在对应子项目内部，不要跨项目复制代码。
- 如果一个需求需要同时改动前端仓与后端仓，先明确主改动面，再把另一侧改动控制在必要最小范围内。

## 建议阅读顺序

1. 根 `README.md`
2. 本文件
3. 目标子项目的 `package.json`
4. 目标子项目的目录结构与局部文档
5. 与当前任务直接相关的页面、组件、路由或接口封装代码

## 常见协作场景

### 新增页面或交互

- 先判断属于 `kw-admin-vue` 还是 `kw-frontend-react`。
- 再确认是否已有可复用组件、布局或请求封装。
- 最后在对应子项目内按既有模式扩展，不要直接新起一套风格。

### 前后端联调

- 先确认调用的是哪个后端仓、哪个业务模块。
- 如果接口字段与页面展示不一致，优先明确是前端适配问题还是后端契约问题。
- 如果需要跨仓改动，记得同步更新接口示例、调用说明或页面文案。

### 调整工程配置

- 优先评估变更是否会影响本地开发、打包产物或部署方式。
- 涉及环境变量、反向代理、静态资源部署时，同步检查 `knowledge-infra` 是否需要配套修改。

## 文档约定

- 如果新增前端子项目、明显调整目录结构，或改变启动/构建方式，请同步更新 `README.md` 与本文件。
- 如果某个子项目后续复杂度明显上升，建议在该子项目目录下补充更近一层的 `AGENTS.md`。
